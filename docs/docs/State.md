# State managment

Form state management is based on Flux pattern, using `useReducer` hook with selector-like context library [use-context-selector](https://www.npmjs.com/package/use-context-selector)